import { Open_Sans } from 'next/font/google'
import localFont from 'next/font/local'

const open_sans = Open_Sans({ weight: '700', style: 'italic', subsets: ['vietnamese'], preload: false })
export { open_sans }